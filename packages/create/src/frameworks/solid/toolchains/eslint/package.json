{
  "scripts": {
    "lint": "eslint",
    "format": "prettier --check .",
    "check": "prettier --write . && eslint --fix"
  },
  "devDependencies": {
    "@tanstack/eslint-config": "latest",
    "prettier": "^3.8.1"
  }
}
